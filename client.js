console.log('JS'); //Make sure JS is sourced

$(document).ready(onReady);

function onReady() {
    console.log('Page is loaded');
    $('firstName').val('');
    $('lastName').val('');
    $('emplyeeID').val('');
    $('jobTitle').val('');
    $('firstName').val('');
    $('annualSalary').val('');
}

